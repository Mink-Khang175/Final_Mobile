<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_reset_password_dialog"
    android:orientation="vertical"
    android:paddingHorizontal="22dp"
    android:paddingTop="22dp"
    android:paddingBottom="18dp">

    <FrameLayout
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/bg_home_soft_red_chip"
        android:padding="15dp">

        <androidx.appcompat.widget.AppCompatImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Khôi phục mật khẩu"
            android:src="@drawable/ic_email_24"
            app:tint="@color/red_energy" />
    </FrameLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="sans-serif-condensed"
        android:gravity="center"
        android:includeFontPadding="false"
        android:letterSpacing="0"
        android:text="Khôi phục mật khẩu"
        android:textColor="@color/stadium_ink"
        android:textSize="27sp"
        android:textStyle="bold|italic" />

    <TextView
        android:id="@+id/tvResetPasswordMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="Nhập email đã đăng ký. Hệ thống sẽ gửi liên kết đặt lại mật khẩu vào hộp thư của bạn."
        android:textColor="@color/slate_caption"
        android:textSize="14sp" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/tilResetEmail"
        style="@style/AuthTextInputLayout"
        android:layout_marginTop="22dp"
        android:hint="Email tài khoản"
        app:startIconDrawable="@drawable/ic_email_24">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/edtResetEmail"
            style="@style/AuthTextInputEditText"
            android:inputType="textEmailAddress"
            tools:text="user@example.com" />
    </com.google.android.material.textfield.TextInputLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSendResetEmail"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:layout_marginTop="18dp"
        android:text="GỬI LIÊN KẾT"
        android:textColor="@color/white"
        android:textSize="13sp"
        android:textStyle="bold"
        app:backgroundTint="@color/red_energy"
        app:cornerRadius="14dp"
        app:elevation="0dp" />

    <TextView
        android:id="@+id/btnCancelReset"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/bg_success_secondary_button"
        android:gravity="center"
        android:text="ĐỂ SAU"
        android:textColor="@color/stadium_ink"
        android:textSize="13sp"
        android:textStyle="bold" />
</LinearLayout>
